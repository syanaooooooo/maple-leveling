# 冒险岛升级计划 开发规则

## 项目信息
- **本地路径**：`/Users/o/Desktop/code/maple-leveling/`
- **GitHub Pages**：`https://syanaooooooo.github.io/maple-leveling/`
- **工作分支**：`main`（直接推 main，自动部署）
- **本地服务器**：`python3 -m http.server 8891`
- **push 惯例**：`git add -A && git commit -m "..." && git push origin main`
- **缓存**：改 style.css / app.js / sync.js 时，index.html 里对应的 `?v=N` 加一
  - ⚠️ 改完 **grep 一下确认真的变了**。用 sed/replace 按旧值替换时，值对不上会静默失败 ——
    app.js 曾经卡在 `?v=5` 好几轮没人发现，线上一直吃缓存

## 沟通方式
- 所有讨论用中文；代码注释、commit message 可中英混用
- 先 scope 后动手；改动前确认；不过度开发

## ⛔ 测试数据的铁律（踩过一次大坑）

- **绝对不要**在调试时往 `mls_main`（用户真实数据那一行）写空状态或测试数据。
  2026-09-22 就是这么把用户的角色数据全清了 —— 本地清空 + 推云端，
  用户另一台机器打开拉到空数据，同步回来把两边都盖了，数据库没有备份可捞。
- 要测同步就**换个行名**（比如 `mls_devtest`），或者只动 localStorage 不碰云端。
- 清理测试数据时，如果非要写云端，`updated_at` 要写成很早的时间（如 `2000-01-01`），
  这样任何有真实数据的客户端都会赢过它，不会被反向覆盖。

## 数据
- Supabase 项目同 fitness-tracker / bobing 共用，但**这个 app 不用共用的 `snapshots` 表**
- 主数据：共用的 `user_data` 表，`app='maple'`、`name='main'`，唯一键 `(user_id, app, name)`，开了 RLS
- 一个账号管所有 app，baby-health 等也在这张表里（各自 `app` 不同）
- 建表 SQL 在 `supabase-setup.sql`
- ⚠️ `app_data` 这个名字被 baby-food-tracker 的单行表占了，别搞混
- localStorage key：`mls_v1`（退出登录时会清掉）
- 同步策略：比 `updated_at`，新的赢；本地写入后 600ms debounce 推云端
- **空云端绝不覆盖有数据的本地**：`boot()` 里如果云端 `chars` 为空而本地非空，
  保留本地并反推上云。空状态几乎总是出岔子，不该被当成「最新」
- **本机备份** `mls_backup`：启动时、每次保存、被云端覆盖前各存一份，留最近 5 份，
  纯本地不上云。一个角色都没有时页面顶部会出现找回入口

## 经验表
- `exp-table.js` 里 `EXP[L]` = L 级升 L+1 级所需经验，L = 1…199
- 数值是国服怀旧服（CMS079），和大爆炸后的国际服经验表**完全不同**，别混用
- 改这个表前先跟用户确认版本

## 地图表
- `maps.js` 里 `MAPS` = [{ region, maps:[{n, id}] }]，按地区分组
- 来源 mxdzlk.com/map/，怀旧服目前只开放到维多利亚岛，共 272 张图
- 新版本开新大陆时要重新抓一次

## 图表
- `chartHTML(points)` 画的是折线图：横轴 = 计时走过的真实时间（按间隔定位，不是等距），
  纵轴 = 那一段的经验/小时
- Y 轴**按数据自身范围自适应**，上下各留 12%，不硬从 0 起 —— 从 0 起会把起伏压成一条平线
- 值跨 0 时画一条红色虚线标 0；刻度用 `fmtAxis(v, span)` 按量程挑精度，免得小范围下刻度重复
- 贴边的文字要用**内联 `style="text-anchor:..."`**，SVG 里 CSS 的 `text-anchor` 会盖掉同名呈现属性
- 窄屏下图不缩小，`.cchart` 有 `min-width:520px`，靠横向滚动保证字看得清

## 效率记录（tab）
- **主键是「等级 + 地图」** —— `levelMapStats()` 把所有 session 的段按
  `(段开始时的等级, 段的地图)` 汇总，按时长加权算经验/小时
- 段的等级取 `points[i-1].level`（开始时），和地图取自同一个打点，两者对得上
- 排序：**等级高的在上；同一级按累计经验多的在上**
- 原来只按地图排的「地图效率排行」已经被它取代，删了
- tab 状态存 localStorage `mls_tab`，刷新后停在上次那个

## ⚠️ 计划的单位是「级数」不是经验

整套排期以**浮点等级**为单位（`levelFloat`：Lv.43 打了 98% = 43.98），不是经验值。

- `lvPerDay` = 还要升的级数 ÷ 剩余天数 —— 这是计划的核心数字，显示成「每天要打 31.9% 一级」
- 换算成经验只是为了显示：`expFromTo(cur, advanceLevels(cur, lvPerDay))`
- `advanceLevels(lv, exp, n)` 按级数前进，跨级时余下的零头按**新等级**的经验量算
  （「一级」是个单位，不管这一级本身要多少经验）
- 超前/落后、按当前速度的 ETA、升级明细的预计日期，**全部按级数算**，别混回经验
- ⚠️ **速度和「应该完成多少」的分母用小数天 `elapsedDays`，不是整数天 `elapsed`**。
  整数天会把今天打的算进分子、却不算进分母 —— 计划第二天的上午，一天半的战果
  除以 1 天，「实际每天」虚高五成，ETA 跟着乐观三天（实测 89.7% → 60.4%，10-01 → 10-04）。
  `elapsed` 只留给「已过 N 天」这类显示
- 不足 6 小时（`elapsedDays < 0.25`）不算速度，否则刚建好计划打五分钟会推出「明天就达成」

**为什么不用经验**：等级越高每只怪给的经验越多。固定经验/天 → 练级时间逐日递减，
前松后紧对不上实际。固定级数/天 → 每日经验需求递增（Lv.43 时 13.6万，Lv.50 时 22.6万），
但同期怪的经验也在涨，实际练级时间才是平的。

## 总进度面板的主次（别再堆平）
一屏之内只让**一个**东西最大：**今天还要打多少**。其余按「每天打开时要回答什么」降级：

1. 今天够不够 → `.bar.day.big`，40px 高、24px 数字，唯一的主角
2. 超前/落后 → 右上角徽章（判断，不是操作）
3. 每天要打 → hero 数字卡
4. 整体进度 → `.overall` 一行细条，和「0.90 / 7.28 级」并排，是背景不是主角
5. 剩余经验、原计划日均、已打经验 → 面板底部一行小字

**次级信息只允许一层，且每层只有一行**（`.say` / `.overall-v` / `.stat .n` 同字号 17px、同颜色）。
原来今日区有两行小字（19px 深色 + 16px 浅色），底下面板还有第三种 —— 说的是同一件事却长得不一样。

**加一行之前先查重**：底部那行「剩余经验 · 原计划每天 · 已打经验」里，
`原计划每天` 在「目标设置」已有、`已打经验` 和整体行的「0.90 / 7.28 级」是同一件事换单位，
三个数只有剩余经验是独有的 —— 整行删掉，剩余经验并进整体行。

**两条铁律**：
- **今日的目标和已完成必须同单位**（都用「% 一级」）。混着「% 一级」和「万经验」，
  读者没法一眼看出进度条那个百分比是怎么来的
- **等级只在身份行说一次**。今日区不要再写「现在 Lv.xx」

⚠️ 超额时**不能用箭头**写「现在 A → 今天要到 B」—— B 会小于 A，箭头指向更小的数字，
读起来像倒退。超额说「今天的线是 B，已经过了」，没达标说「再打到 B 就达标」。

## 今日计划进度
- `compute()` 里的 `todayGain` / `todayNeed` / `todayPct`
- 起点 = 今天之前最后一条打卡（没有就用计划起点）；现在 = 今天的打卡，
  计时器里有更靠前的打点就用打点（练级中进度条也会动）
- **今日目标用「今天开始时的位置」算**，一天之内不会因为你打了而缩水
- `todayLevels` = 今天该升几级；`dayGoalPos` = 今天该到的位置；`todayNeed` 是由它反推的经验
- 允许超过 100%：条填满后变绿，文案显示超出多少
- **显示用「等级 + 本级百分比」而不是绝对经验值**（`advance()` / `fmtPos()`）：
  游戏里就是这么显示的，好对照；而且等级越高每只怪经验越多，绝对经验值不直观。
  底下的算术还是按经验算的，只是换了个说法

## 输入框
- 百分比输入框带 `.pctin`：**直接敲数字，小数点自动补上**
  （敲 1 1 5 6 依次是 0.01 → 0.11 → 1.15 → 11.56，退格按显示字符走，像收银机）
- 这不只是省事：以前要手打小数点，很容易留着上一次的值就按了「记一笔」，
  记进一个倒退的打点，效率图上就会出现巨大的负值
- 单位切到「经验值」时不带 `.pctin`，按整数输入
- 所有数字框带 `.numin`：聚焦时光标自动落到末尾

## 计时器数据结构
- `c.timer` = { state:'running'|'paused', startedAt, accumMs, points:[{ms, level, exp, map, at}] }
  - `ms` 是有效计时毫秒，暂停期间不累加；`startedAt` 是 epoch，刷新页面不影响计时
  - 一段 = points[i-1] → points[i]，**这段的地图取 points[i-1].map**（打点时选的是「接下来在哪练」）
- `c.sessions` = 终止后的存档，带 points 快照，能重画图表
- ⚠️ **暂停期间计时不走，这时再「记一笔」必须改上一笔而不是新增**。
  新增会造出 `dt=0` 的段，`rateOf` 返回 0，那点经验就从效率图里消失了
  （总数还在，所以症状是「这段共打 6767」但「最高 0/h」）。
  `tmMark` 里判断 `now - last.ms < 1000` 就原地改；`segmentsOf` 也会把
  历史数据里 `dt<=0` 的段并进上一段兜底
- `tmUndo()` 撤销最后一个打点（起点那笔撤不掉）。确认框里会写清楚要撤的是哪一笔、
  那一段记了多少经验 —— 记错的打点会让效率图出现巨大负值，撤销是最快的补救

## 登录与安全
- **Supabase Auth（邮箱 + 密码）**。账号和 baby-health / baby-food / emotion-dial 共用一个
- **源代码里不能出现任何密码或密码哈希**。密码只在输入框里存在，直接发给 Supabase 校验
- `user_data` 开了 RLS，策略 `auth.uid() = user_id` —— 没登录读不到任何东西
- `supabase-config.js` 里的 anon key 是设计成公开的，不是秘密，别为了藏它折腾
- 账号在 Supabase 后台 Authentication → Users 手动建，页面上不开放注册
- 退出登录会清掉 localStorage 里的 `mls_v1`
- 历史：原来是软密码门禁（`PASS_HASH` 硬编码），2026-09-22 换掉了
